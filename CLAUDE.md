# Full Court (NBA Morning Brief) — Project Reference

## מה זה
כלי שרץ כל בוקר (שעון ישראל) ומייצר סיכום קריא של לילה של NBA: תוצאות המשחקים שהסתיימו
בלילה, רגעים בולטים, ביצועים חריגים, והקשר רחב יותר (סדרות פלייאוף, שלבי גביע, פלייאין,
פציעות). מיועד לקריאה מהטלפון עם קפה בבוקר. עברית בלבד, RTL מלא, mobile-first, ערכת נושא
בהירה/כהה (עם toggle ידני).

## מקור נתונים
**nba_api** (פייתון, קוד פתוח, https://github.com/swar/nba_api) - עוטף endpoints לא-רשמיים
של `stats.nba.com`. חינמי לגמרי, בלי API key. self-throttling של ~1 בקשה/שנייה בין קריאות
(`REQUEST_DELAY_SECONDS` ב-`fetch.py`).

מודולים עיקריים בשימוש: `ScoreboardV3` (משחקים/תוצאות/סטטוס חי + `gameTimeUTC`),
`BoxScoreTraditionalV3` (בוקס סקור מלא, כולל `comment` ל-DNP), `LeagueStandingsV3`
(טבלה), `LeagueGameFinder` (שחזור סדרות פלייאוף), `ScheduleLeagueV2` (לוח משחקים מלא של
העונה - `gameId` prefix קובע סוג: `"002"`=עונה רגילה/גביע (בתים+רבע/חצי-גמר), `"004"`=
פלייאוף, `"005"`=פלייאין, `"006"`=גמר הגביע, `"001"`=פרה-סיזון, `"003"`=אולסטאר),
`LeagueDashPlayerStats` (סטטיסטיקות עונתיות לכל שחקן - בסיס לדירוג העוצמה).

מקורות משלימים לא-רשמיים (ESPN, אותה רוח בדיוק כמו nba_api - בלי מפתח, בלי הרשמה):
`site.api.espn.com/.../nba/injuries` (דוח פציעות - סנאפשוט חי, לא לפי תאריך).

**⚠️ מגבלה קריטית שהתגלתה**: `stats.nba.com` חוסם/לא עונה לבקשות שמגיעות מ-IP של ספקי ענן
(GitHub Actions, AWS, Azure וכו') - נבדק ואומת פעמיים. **המשמעות**: שליפת הנתונים חייבת לרוץ
ממחשב עם אינטרנט ביתי/רגיל, אף פעם לא מ-CI/cloud runner. ראו "אחסון והפצה" למטה.

**timezone**: "אתמול" בשעון US Eastern (דרך `zoneinfo`, ללא המרה לישראל) - הכלי רץ פעם ביום
אחרי שכל משחקי הלילה הסתיימו, אז אין צורך בהמרה זהירה בין אזורי זמן.

## מבנה הפרויקט
```
full-court/
├── CLAUDE.md                    # קובץ זה
├── config.py                    # timezone + season-string helpers
├── fetch.py                     # שליפת games/box_scores/standings/playoff_series/cup/play_in
├── storylines.py                # זיהוי סיפורים מעניינים מהנתונים הגולמיים
├── summarize.py                 # קריאה ל-Claude API לכתיבת הסיכום בעברית
├── render.py                    # בניית ה-HTML הסופי (TEMPLATE + כל ה-_build_*_html)
├── main.py                      # מריץ את כל הצינור פעם אחת (ידני/דיבוג)
├── scheduler.py                 # check() - נקודת הכניסה לריצה מתוזמנת (ראו "תזמון" למטה)
├── _generate_assets.py          # יוצר favicon/logo (לוגיקת ציור "כדור" + טקסט)
├── _generate_pwa_icons.py       # יוצר אייקוני PWA (icon-192/512/180) מאותה לוגיקת כדור
├── _generate_demo_index.py      # בונה output/demos.html (עמוד דמו זמני, ראו למטה)
├── demo*.py + demo*_fixture.json  # 11 תרחישי דמו (JSON קפוא + סקריפט טעינה) לכל סוג יום
├── requirements.txt
├── .env                         # ANTHROPIC_API_KEY (לא ב-git)
├── .github/workflows/daily-brief.yml  # workflow פרסום ל-Pages (לא מריץ פייתון, ראו למטה)
└── output/
    ├── {YYYY-MM-DD}.html        # בריף יומי
    ├── index.html               # עותק של הבריף האחרון - הכתובת היציבה שה-PWA מצביע עליה
    ├── manifest.json, assets/   # PWA + עיצוב (תמונות, אייקונים)
    └── demos.html               # רשימת דמואים זמנית (offseason בלבד, ראו למטה)
```

## אחסון והפצה
**הבעיה**: ניסיון ראשוני להריץ את כל הצינור (fetch + Claude) בתוך GitHub Actions נכשל
בעקביות - `stats.nba.com` חוסם בקשות מספקי ענן (ראו "מגבלה קריטית" למעלה).

**הארכיטקטורה הנוכחית** (פיצול תפקידים):
- **החישוב** (fetch + סיכום Claude, דרך `scheduler.py`) רץ **מבית** - כרגע מהמחשב האישי
  (Windows Task Scheduler, כבוי כרגע - ראו "משימות פתוחות" למטה), בעתיד מ-Raspberry Pi
  ייעודי. שני המקרים "יורשים" את מוניטין האינטרנט הביתי, שעובד מול `stats.nba.com` בלי
  חסימה. **פתוח**: כשה-Pi יגיע - להתקין פייתון+תלויות, להעביר `.env`, להגדיר cron במקום
  Task Scheduler, לוודא גישת git push (SSH key/credential helper).
- בסוף כל ריצה מוצלחת, `scheduler.py`'s `_publish_to_github()` עושה `git add`+`commit`+
  `push` ל-`output/`.
- **GitHub Actions** (`.github/workflows/daily-brief.yml`) לא מריץ פייתון בכלל - תפקידו
  היחיד הוא `on: push` ל-`output/**`, שמפרסם (`peaceiris/actions-gh-pages`) את מה שנדחף
  ל-branch `gh-pages`, שממנו GitHub Pages מגיש את האתר (`ojbar.github.io/full-court`).
- **PWA**: `output/manifest.json` + אייקונים + תגיות `<head>` (manifest link,
  apple-touch-icon, theme-color בהיר/כהה, apple-mobile-web-app-*) - מאפשר "הוספה למסך
  הבית" באנדרואיד ובאייפון. נבדק בפועל על טלפון אמיתי - עובד. `render.py`'s `save()` כותב
  גם `output/index.html` (עותק של הבריף האחרון) - זו הכתובת היציבה ש-`start_url` מצביע
  עליה. **מגבלת iOS ידועה**: כשה-PWA רץ במצב standalone (מהאייקון), ספארי מבטל את מחוות
  Pull-to-refresh הטבעית (עובד בספארי רגיל, לא ב-standalone) - נפתר עם כפתור רענון גלוי
  (🔄) בכותרת, לצד כפתורי תצוגה/נגישות.
- **repo**: ציבורי, `github.com/OJBAR/full-court`. זהות ה-git commits: כינוי `OJ-Barkley`
  + כתובת noreply של GitHub (לא מייל אמיתי) - כדי לא לחשוף פרטים אישיים ב-repo ציבורי.
- **פידבק/יצירת קשר**: פופאפ "יצירת קשר" בפוטר, שתי אפשרויות ממוסגרות - GitHub Issues של
  ה-repo, ומייל (`mailto:ojbar30@gmail.com` עם subject קבוע).
- **דמו זמני** (`output/demos.html`, נוצר ע"י `_generate_demo_index.py`): רשימה ממוינת
  מהכי מוקדם להכי מאוחר של 11 תרחישי הדמו, מסווגת לאחת משש קטגוריות (רגיל/גביע בתים/גביע
  נוקאאוט/פלייאוף/גמר NBA/פלייאין) לפי דגלי הנתונים האמיתיים בכל fixture - לא ניחוש
  מהטקסט. אותה מערכת CSS (בהיר/כהה) כמו שאר האתר. קישור אליו בפוטר כל עמוד ("גרסת בטא ·
  מעבר בין דמואים") - **זמני**, לתקופת ה-offseason בלבד כדי לבדוק ששרשרת הפרסום עובדת
  ולתת לחברים לדפדף בין תרחישים; ייעלם כשהעונה האמיתית תתחיל ויהיה תוכן אמיתי במקום.

## תזמון (scheduler.py)
`check()` מיועדת לרוץ כל ~20 דקות (Task Scheduler/cron) - כל הרצה היא בדיקה קצרה וחסרת
מצב: "האם הבריף של אתמול בלילה מוכן?" בלי sleep/לולאה פנימית - קצב הבדיקה מגיע מהטריגר
החיצוני, לא מקוד שמחכה.

- **אידמפוטנטיות**: אם `output/{date}.html` כבר קיים - יוצא מיד.
- **אין משחקים בלילה היעד** - לא רץ בכלל, בלי בריף ובלי קובץ.
- **בדיקה ראשונה** ב-(שעת התחלת המשחק האחרון + 2:30 שעות) - נשלף מ-`gameTimeUTC`.
  לפני זה `check()` יוצא מיד. אחרי זה כל הרצה זהה (אין הבדל בין "בדיקה שנייה" ל"עשירית").
- **לא מנחש משך משחק** - בודק בכל פעם את הסטטוס האמיתי מול ה-API, אז לא יכול "לפספס"
  משחק שנמשך הרבה זמן (גם הארכות מרובות).
- **רצפה**: הבריף לעולם לא נשלח לפני 05:30 בבוקר שעון ישראל.
- **דחיית משחק**: אם `gameStatusText` מראה דחייה/ביטול - מוציא מהרשימה מיד, לא ממתין.
- **תקרת בטיחות**: 6 שעות אחרי הבדיקה הראשונה - אם משחק תקוע בסטטוס לא-מזוהה, שולח בלעדיו
  ומציין זאת, לא נתקע לנצח.
- **פרסום**: בסוף ריצה מוצלחת, `_publish_to_github()` עושה git add/commit/push (ראו
  "אחסון והפצה" למעלה). כשל בפרסום רק נרשם ללוג - לא מפיל את הריצה (הבריף כבר נשמר מקומית).

**Windows Task Scheduler**: משימה בשם `FullCourtBrief` קיימת ומוגדרת (`venv\Scripts\
python.exe scheduler.py`, כל 20 דקות, ללא הגבלת זמן) אבל **במצב Disabled בכוונה** - לא
תרוץ עד שיינתן אישור מפורש להפעיל (`Enable-ScheduledTask -TaskName "FullCourtBrief"`).

## פ'יצרים לפי סוג יום

### עונה רגילה
טבלת ליגה (מזרח/מערב, ממוינת לפי `PlayoffRank`, כולל סטריק). Storylines: משחקים צמודים/
הארכה, ביצועים חריגים (30+ נק', טריפל-דאבל, 15+ ריב'/אסיסט), הפתעות (קבוצה מדורגת נמוך
שניצחה גבוה), סטריקים בולטים.

### NBA Cup (Emirates NBA Cup)
כל משחק מתויג ברמת משחק בודד (`cup_subtype`: `"in-season"`=בתים, `"in-season-knockout"`
=רבע גמר ומעלה). טבלת הליגה הרגילה מוצגת תמיד בימי גביע (חוץ מהגמר, כל משחקי הגביע נספרים
בעונה הרגילה).
- **שלב הבתים**: טאב "בתי הגביע" - `Group A/B/C` לכל קונפרנס, מאזן+הפרש סלים בגביע בלבד,
  קו מפריד בין מקום 1 ל-2. מסומנת ה-Wild Card (הסגנית עם המאזן הטוב ביותר בקונפרנס, עולה
  גם היא לנוקאאוט) בתג "WC".
- **שלב הנוקאאוט**: טאב "בראקט הגביע" - בראקט מחובר אמיתי (רבע-גמר→חצי-גמר→גמר), תמיד
  מציג את הצורה המלאה (TBD למשבצות לא-מוכרעות). כותרות עמודות באנגלית ("Quarterfinals/
  Semifinals/Championship"). ה-wins/losses שמדווח ה-API בשלב הנוקאאוט הם **מוגבלים לשלב
  הנוקאאוט עצמו** (למשל 1-0), לא מאזן העונה - בטאב התוצאות מוצג המאזן האמיתי (נשלף מהטבלה).

### פלייאוף
שני טאבים נפרדים - **"בראקט מערב"** ו**"בראקט מזרח"** (מערב קודם) - כל אחד בראקט מחובר
אמיתי מסיבוב 1 עד גמר הקונפרנס (1st Round→Conf. Semifinals→Conf. Finals), מוצג תמיד מרגע
שסיבוב 1 מתחיל. הזיווג בסיבוב 1 (1נגד8+4נגד5 בחצי אחד; 2נגד7+3נגד6 בחצי השני) הוא מבנה
בראקט אמיתי, לא שרירותי. כל תא מציג מספר seed (בתג ממוסגר) ליד קוד הקבוצה, והקבוצה עם
**יתרון הבית** (seed נמוך יותר) מוצגת תמיד למעלה - לא לפי מי מוביל כרגע בניצחונות.
**גמר NBA**: טאב "גמר ה-NBA" מופיע ראשון כשיש סדרת גמר פעילה (הבראקטים האזוריים ממשיכים
להופיע גם כן, כהיסטוריה). התגים בתא הזה הם W/E צבעוניים (לא seed - לא ניתן להשוואה בין
קונפרנסים), עם הודעת "אלופת ה-NBA!" כשהסדרה מוכרעת.
**"תוצאת המשחק"**: כשיש רק משחק אחד בלילה (נפוץ בפלייאוף/גמר), כותרת האקורדיון הראשון
משתנה בהתאם, וגם הטקסט החופשי (הנחיה ב-SYSTEM_PROMPT) כותב בלשון יחיד.

### פלייאין
שני טאבים נפרדים - "פלייאין מערב"/"פלייאין מזרח" (סדר הטאבים בכל יום כזה: תוצאות → פלייאין
מערב → פלייאין מזרח → טבלת הליגה). מוצג כבראקט: 7-מול-8 ו-9-מול-10 מתכנסים למשחק הכרעה.
זיהוי משחק פלייאין: `game_id` מתחיל ב-`"005"` (`poRoundDesc` ריק אצלו, אז `is_playoffs`
נשאר False). זיהוי שלושת המשחקים לפי `seed` הקבוצות, לא לפי תאריך (הקונפרנסים לא תמיד
משחקים באותו לילה). **הבראקט לא סימטרי**: מנצח 7-מול-8 מקבל seed 7 ומסיים (לא ממשיך) -
המפסיד הוא זה שממשיך למשחק ההכרעה. לכן כל תא מקבל caption קטן מתחתיו שמסביר את המשמעות
המדויקת. `get_play_in_bracket()` ב-`fetch.py` משחזר את **כל** משחקי הפלייאין של העונה עד
התאריך המבוקש (לא רק לילה בודד) - כדי שבלילה של ההכרעה, שני המשחקים הקודמים לא יוצגו
כ-TBD. אין משחק "if necessary" - תמיד בדיוק 3 משחקים קבועים לכל קונפרנס. אין מאזן עונה
מוצג בטאב התוצאות לימי פלייאין (לא רלוונטי). `summarize.py` מקבל הקשר מובנה
(`play_in_context` עם `game_type`: `seven_vs_eight`/`nine_vs_ten`/`decider`) כדי לכתוב
משפט נכון על מה בדיוק על הפרק.

### הקשר מעבר למשחק (פציעות + כוכבים חלשים)
`get_injuries()` (ESPN) ו-`get_player_power_ratings()` (נוסחת "ESPN Rating" מחושבת
מ-`leaguedashplayerstats`: `PTS+REB+1.4*AST+STL+1.4*BLK-.7*TOV+FGM+.5*FG3M-.8*(FGA-FGM)
+.25*FTM-.8*(FTA-FTM)`) - שניהם עם fallback שקט (רשימה ריקה) אם מקור לא-רשמי נופל.
`find_injury_notes` (שחקן `Out` מקבוצה ששיחקה) ו-`find_star_weak_performances` (שחקן
בין 30 המובילים בדירוג שקלע חצי או פחות מהממוצע העונתי **שלו עצמו**). הדירוג המשוקלל
משמש רק פנימית לזיהוי "כוכב" - **לא** נכלל ב-JSON שנשלח ל-Claude ואסור לציין מספר דירוג
בטקסט. **לא ממומש**: זיהוי "חזרה מפציעה אחרי היעדרות ארוכה" או "נפצע באמצע המשחק" - דורש
מקור נתונים נוסף (game log/עדכון בזמן אמת) שלא קיים כרגע.

### נגישות
כפתור `.a11y-toggle` (♿) פותח פאנל עם 3 רמות גודל טקסט (zoom על ה-wrapper), focus trap
מלא (Tab לא בורח, Escape סוגר). Landmarks סמנטיים (`header`/`main`/`footer`), `:focus-
visible` על כפתורים/קישורים, `.sr-only`. קישור "הצהרת נגישות" בתחתית הפאנל (מה שבוצע
בפועל, בלי הצהרת פטור משפטית - זה כלי אישי לא שירות מסחרי).

### כללי ניסוח וכתיב (תהליך מתמשך)
`summarize.py`'s SYSTEM_PROMPT כולל מילון תעתיק קבוע לשמות כל 30 הקבוצות ולשחקנים נפוצים
(יאניס אנטטקומפו לא ג'יאניס, סלטיקס לא קלטיקס, דנבר לא דנוור וכו'), בחירות מילים ("רצף"
לא "סטריק", "מומנטום" נשאר), הנחיה לצמצם קישוטי לשון מיותרים, ועובדות קבועות (גמר הגביע
תמיד ב-T-Mobile Arena לאס וגאס). מתעדכן ככל שמגיעים תיקונים נוספים.

## החלטות
- שפת הפלט: עברית בלבד.
- ערוץ הפצה: PWA מאורח ב-GitHub Pages (לא מייל/Slack/Telegram).
- קבוצות מועדפות: כרגע כללי - בעתיד אולי העדפות משתמש (קבוצות/שחקנים אהודים).

## משימות פתוחות (Backlog)
לא לבצע עד שיתבקש במפורש:

1. **טאב "מרחף" קבוע** - רעיון גולמי, בלי חזון מלא עדיין: טאב שנשאר תמיד זמין בלי קשר
   לסוג היום (למשל טבלת הליגה). כרגע הכל בתוך אקורדיונים שצריך לפתוח אחד-אחד.
2. **ארכיון עונתי** - חזרה אחורה לעיין בבריפים של ימים קודמים, מסודר לפי תאריכים.
   `output/index.html` מציג תמיד רק את האחרון - `demos.html` הוא פתרון זמני ל-offseason
   בלבד. צריך עמוד ארכיון קבוע שיתעדכן אוטומטית עם כל בריף חדש.
3. **הפעלת Task Scheduler** - המשימה `FullCourtBrief` קיימת אבל Disabled - להפעיל
   (`Enable-ScheduledTask`) כשמחליטים שרוצים ריצה אוטומטית בפועל.
4. **Push Notifications** - התראה לטלפון כשבריף חדש מוכן. היתכן (iOS 16.4+, רק ל-PWA
   מותקן) - חסר Service Worker + מנגנון subscription. יתרון: `scheduler.py` יכול לשלוח
   בעצמו (רץ מהבית עם אינטרנט אמיתי), אין צורך ב-backend נפרד - למשתמש יחיד זה פשוט
   משמעותית ממוצר רב-משתמשים (בלי מסד נתונים/הרשמה אוטומטית/ניקוי מנויים).
5. **טאב "משחקי הלילה הבא"** - תצוגה מקדימה של לוח המשחקים של הלילה הקרוב (לא תוצאות -
   משחקים שעוד לא שוחקו). דורש שליפת נתונים חדשה לגמרי (`fetch.py` היום מושך רק תוצאות
   של לילה שהסתיים) - `ScheduleLeagueV2` כבר בשימוש בקוד למטרות אחרות, אז זה ישים. אין
   חפיפה/חיסכון עבודה מול הבריף של היום שלמחרת (נתון שונה לגמרי - תוצאות מול לוח משחקים).
   שאלות פתוחות: מה בדיוק מוצג (שמות קבוצות בלבד? שעות? המרה לשעון ישראל?).
