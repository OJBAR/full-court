# Full Court (NBA Morning Brief) — Project Reference

## מה זה
כלי שרץ כל בוקר (שעון ישראל) ומייצר סיכום קריא של לילה של NBA: תוצאות המשחקים שהסתיימו
בלילה, רגעים בולטים, ביצועים חריגים, והקשר רחב יותר (סדרות פלייאוף, שלבי גביע, פלייאין,
פציעות). מיועד לקריאה מהטלפון עם קפה בבוקר. עברית בלבד, RTL מלא, mobile-first, ערכת נושא
בהירה/כהה (עם toggle ידני).

## מקור נתונים
**nba_api** (פייתון, קוד פתוח, https://github.com/swar/nba_api) - עוטף endpoints לא-רשמיים
של `stats.nba.com`. חינמי לגמרי, בלי API key. self-throttling של ~1 בקשה/שנייה בין קריאות
(`REQUEST_DELAY_SECONDS` ב-`fetch.py`).

מודולים עיקריים בשימוש: `ScoreboardV3` (משחקים/תוצאות/סטטוס חי + `gameTimeUTC`),
`BoxScoreTraditionalV3` (בוקס סקור מלא, כולל `comment` ל-DNP), `LeagueStandingsV3`
(טבלה - אין בה עמודת tricode, ראו bullet נפרד למטה), `LeagueGameFinder` (שחזור סדרות
פלייאוף), `ScheduleLeagueV2` (לוח משחקים מלא של העונה - `gameId` prefix קובע סוג: `"002"`=
עונה רגילה/גביע (בתים+רבע/חצי-גמר), `"004"`=פלייאוף, `"005"`=פלייאין, `"006"`=גמר הגביע,
`"001"`=פרה-סיזון, `"003"`=אולסטאר), `LeagueDashPlayerStats` (סטטיסטיקות עונתיות לכל שחקן -
בסיס לדירוג העוצמה).

מקורות משלימים לא-רשמיים, אותה רוח בדיוק כמו nba_api (בלי מפתח, בלי הרשמה):
- ESPN (`site.api.espn.com/.../nba/injuries`) - דוח פציעות, סנאפשוט חי, לא לפי תאריך.
- `highlights.py`: תקציר יוטיוב לכל משחק, מהערוץ הלא-רשמי GAMETIME HIGHLIGHTS (לא הערוץ
  הרשמי של ה-NBA, שמפרסם רק סיכום לילה אחד מאוחד בלי per-game video להצמיד אליו). אין API
  שמקשר game_id לסרטון - כל חיפוש הוא פשוט GET של דף החיפוש בערוץ, פירסור אותו
  `ytInitialData` JSON שיוטיוב עצמו מרנדר server-side, התאמה לפי שתי הקבוצות + התאריך
  שמופיעים יחד בכותרת הסרטון.

**⚠️ מגבלה קריטית שהתגלתה**: `stats.nba.com` חוסם/לא עונה לבקשות שמגיעות מ-IP של ספקי ענן
(GitHub Actions, AWS, Azure וכו') - נבדק ואומת פעמיים. **המשמעות**: שליפת הנתונים חייבת לרוץ
ממחשב עם אינטרנט ביתי/רגיל, אף פעם לא מ-CI/cloud runner. ראו "אחסון והפצה" למטה.

**timezone**: שני אזורי זמן, לשני דברים שונים. (1) `config.US_EASTERN` - "אתמול" בשעון US
Eastern, קובע איזה לילה משחקים הכלי בכלל שולף (הכלי רץ פעם ביום אחרי שכל משחקי הלילה
הסתיימו, אז אין צורך בהמרה זהירה יותר מזה). (2) `config.ISRAEL` - קובע איזה יום נחשב "היום"
בלוח התוצאות בצד הלקוח (JS) - תאריך ET אינו בהכרח אותו יום קלנדרי בישראל (משחק שנפתח מאוחר
בערב ET כבר נופל על היום הישראלי הבא), אז כל דבר שמזין "היום" ללוח התוצאות חייב את האזור
הזה, לא את US_EASTERN.

## מבנה הפרויקט
```
full-court/
├── CLAUDE.md                    # קובץ זה
├── config.py                    # timezone (US_EASTERN + ISRAEL) + season-string helpers
├── fetch.py                     # שליפת games/box_scores/standings/playoff_series/cup/play_in
├── storylines.py                # זיהוי סיפורים מעניינים מהנתונים הגולמיים
├── highlights.py                # חיפוש תקציר יוטיוב לכל משחק (GAMETIME HIGHLIGHTS)
├── summarize.py                 # קריאה ל-Claude API לכתיבת הסיכום בעברית
├── render.py                    # בניית ה-HTML הסופי (TEMPLATE + כל ה-_build_*_html)
├── main.py                      # מריץ את כל הצינור פעם אחת (ידני/דיבוג)
├── scheduler.py                 # check() - נקודת הכניסה לריצה מתוזמנת (ראו "תזמון" למטה)
├── _generate_assets.py          # יוצר favicon/logo (לוגיקת ציור "כדור" + טקסט)
├── _generate_pwa_icons.py       # יוצר אייקוני PWA (icon-192/512/180) מאותה לוגיקת כדור
├── _build_curated_demos.py      # בונה את 5 דמואי הבטא עצמם (נתונים+סיכום אמיתיים)
├── _generate_demo_index.py      # בונה output/demos.html (רשימת 5 הדמואים)
├── _fix_curated_demos.py        # מקשר את 5 הדמואים ל-manifest ייעודי משלהם (ראו למטה)
├── _generate_comprehensive_demo.py  # "גרסת מפתח" - כל 212 לילות המשחק האמיתיים של העונה,
│                                 # בלי סיכומי Claude (ראו "גרסת מפתח" למטה)
├── _fix_comprehensive_demo.py   # post-processing על עמודי גרסת המפתח (נתיבים, סרגל ניווט)
├── _comprehensive_cache/        # מטמון מקומי לנתוני גרסת המפתח (לא ב-git, ראו למטה)
├── requirements.txt
├── .env                         # ANTHROPIC_API_KEY (לא ב-git)
├── .github/workflows/daily-brief.yml  # workflow פרסום ל-Pages (לא מריץ פייתון, ראו למטה)
└── output/
    ├── {YYYY-MM-DD}.html        # בריף יומי (5 מהם הם דמואי הבטא, ראו "אחסון והפצה")
    ├── index.html               # עותק של הבריף האחרון - הכתובת היציבה שה-PWA מצביע עליה
    ├── manifest.json, assets/   # PWA + עיצוב (תמונות, אייקונים) של האתר האמיתי
    ├── demos.html, demos-manifest.json  # עמוד עיון + manifest ייעודי ל-5 דמואי הבטא
    └── comprehensive/           # "גרסת מפתח" - מוסתרת, לא מקושרת משום דף אחר (ראו למטה)
```

## אחסון והפצה
**הבעיה**: ניסיון ראשוני להריץ את כל הצינור (fetch + Claude) בתוך GitHub Actions נכשל
בעקביות - `stats.nba.com` חוסם בקשות מספקי ענן (ראו "מגבלה קריטית" למעלה).

**הארכיטקטורה הנוכחית** (פיצול תפקידים):
- **החישוב** (fetch + סיכום Claude, דרך `scheduler.py`) רץ **מבית** - כרגע מהמחשב האישי
  (Windows Task Scheduler, כבוי כרגע - ראו "משימות פתוחות" למטה), בעתיד מ-Raspberry Pi
  ייעודי. שני המקרים "יורשים" את מוניטין האינטרנט הביתי, שעובד מול `stats.nba.com` בלי
  חסימה. **פתוח**: כשה-Pi יגיע - להתקין פייתון+תלויות, להעביר `.env`, להגדיר cron במקום
  Task Scheduler, לוודא גישת git push (SSH key/credential helper).
- בסוף כל ריצה מוצלחת, `scheduler.py`'s `_publish_to_github()` עושה `git add`+`commit`+
  `push` ל-`output/`.
- **GitHub Actions** (`.github/workflows/daily-brief.yml`) לא מריץ פייתון בכלל - תפקידו
  היחיד הוא `on: push` ל-`output/**`, שמפרסם (`peaceiris/actions-gh-pages`) את מה שנדחף
  ל-branch `gh-pages`, שממנו GitHub Pages מגיש את האתר (`ojbar.github.io/full-court`).
- **PWA**: `output/manifest.json` + אייקונים + תגיות `<head>` (manifest link,
  apple-touch-icon, theme-color בהיר/כהה, apple-mobile-web-app-*) - מאפשר "הוספה למסך
  הבית" באנדרואיד ובאייפון. נבדק בפועל על טלפון אמיתי - עובד. `render.py`'s `save()` כותב
  גם `output/index.html` (עותק של הבריף האחרון) - זו הכתובת היציבה ש-`start_url` מצביע
  עליה. **מגבלת iOS ידועה**: כשה-PWA רץ במצב standalone (מהאייקון), ספארי מבטל את מחוות
  Pull-to-refresh הטבעית (עובד בספארי רגיל, לא ב-standalone) - נפתר עם כפתור רענון (🔄)
  בתוך פאנל ההגדרות (ראו "הגדרות ונגישות" למטה).
- **repo**: ציבורי, `github.com/OJBAR/full-court`. זהות ה-git commits: כינוי `OJ-Barkley`
  + כתובת noreply של GitHub (לא מייל אמיתי) - כדי לא לחשוף פרטים אישיים ב-repo ציבורי.
- **פידבק/יצירת קשר**: בתוך פאנל ההגדרות, שתי אפשרויות ממוסגרות - GitHub Issues של ה-repo,
  ומייל (`mailto:ojbar30@gmail.com` עם subject קבוע).
- **דמואי בטא** (`output/demos.html`, נוצר ע"י `_generate_demo_index.py`): 5 דמואים -
  **כולם נתונים אמיתיים מהעונה 2025-26, כולל סיכום Claude אמיתי לכל אחד** (לא טקסט ממולא)
  - נבנים בפועל ע"י `_build_curated_demos.py` (רשימת 5 התאריכים והנימוק לכל אחד נמצאים
  בתיעוד של אותו קובץ). אחד לכל סוג יום: עונה רגילה, בתי גביע, נוקאאוט גביע, פלייאין,
  פלייאוף. יושבים באותם נתיבים (`output/{date}.html`) שבריף אמיתי ישתמש בהם יום אחד, אז
  יש להם manifest ייעודי משלהם (`output/demos-manifest.json`, `_fix_curated_demos.py`
  מקשר אליו) - אחרת "הוספה למסך הבית" מכל אחד הייתה תמיד פותחת את `index.html` הכי עדכני
  (העתק שריצת `render.save()` האחרונה כתבה), לא את הדמו הספציפי שהותקן ממנו. קישור לעמוד
  מפאנל ההגדרות ("מעבר בין דמואים") - להראות לחברים ולקבל פידבק לפני שהעונה האמיתית מתחילה.

  **גרסת מפתח** (`output/comprehensive/`, נוצר ע"י `_generate_comprehensive_demo.py` +
  `_fix_comprehensive_demo.py`, גישה רק למפתח - מוסתרת, לא מקושרת משום דף אחר): כל 212
  לילות המשחק האמיתיים של העונה, ניתן לדפדף בין תאריכים דרך סרגל ניווט קבוע בתחתית העמוד.
  בלי סיכום Claude אמיתי (טקסט ממולא קבוע במקום, כדי לא לשלם על 212 קריאות API) - אבל עם
  טבלת ליגה/סטריקים אמיתיים **נכון לאותו תאריך בדיוק** (לא הטבלה הנוכחית - ראו
  `fetch.compute_standings_as_of`), ותקצירי יוטיוב אמיתיים לכל משחק היסטורי שכבר שוחק (לא
  רק ללילה של אותו עמוד עצמו - ראו `enrich_full_history` באותו קובץ). מטמון מקומי
  (`_comprehensive_cache/`, לא ב-git) שומר את הנתונים הגולמיים של כל תאריך כדי שריצה חוזרת
  (למשל אחרי שינוי ב-`render.py`) תהיה רינדור זול בלבד, בלי לשלוף הכול מחדש.

## תזמון (scheduler.py)
`check()` מיועדת לרוץ כל ~20 דקות (Task Scheduler/cron) - כל הרצה היא בדיקה קצרה וחסרת
מצב: "האם הבריף של אתמול בלילה מוכן?" בלי sleep/לולאה פנימית - קצב הבדיקה מגיע מהטריגר
החיצוני, לא מקוד שמחכה.

- **אידמפוטנטיות**: אם `output/{date}.html` כבר קיים - יוצא מיד.
- **אין משחקים בלילה היעד** - לא רץ בכלל, בלי בריף ובלי קובץ.
- **בדיקה ראשונה** ב-(שעת התחלת המשחק האחרון + 2:30 שעות) - נשלף מ-`gameTimeUTC`.
  לפני זה `check()` יוצא מיד. אחרי זה כל הרצה זהה (אין הבדל בין "בדיקה שנייה" ל"עשירית").
- **לא מנחש משך משחק** - בודק בכל פעם את הסטטוס האמיתי מול ה-API, אז לא יכול "לפספס"
  משחק שנמשך הרבה זמן (גם הארכות מרובות).
- **רצפה**: הבריף לעולם לא נשלח לפני 05:30 בבוקר שעון ישראל.
- **דחיית משחק**: אם `gameStatusText` מראה דחייה/ביטול - מוציא מהרשימה מיד, לא ממתין.
- **תקרת בטיחות**: 6 שעות אחרי הבדיקה הראשונה - אם משחק תקוע בסטטוס לא-מזוהה, שולח בלעדיו
  ומציין זאת, לא נתקע לנצח.
- **פרסום**: בסוף ריצה מוצלחת, `_publish_to_github()` עושה git add/commit/push (ראו
  "אחסון והפצה" למעלה). כשל בפרסום רק נרשם ללוג - לא מפיל את הריצה (הבריף כבר נשמר מקומית).

**Windows Task Scheduler**: משימה בשם `FullCourtBrief` קיימת ומוגדרת (`venv\Scripts\
python.exe scheduler.py`, כל 20 דקות, ללא הגבלת זמן) אבל **במצב Disabled בכוונה** - לא
תרוץ עד שיינתן אישור מפורש להפעיל (`Enable-ScheduledTask -TaskName "FullCourtBrief"`).

## פ'יצרים לפי סוג יום

### לוח התוצאות (תמיד הטאב הראשון)
`_build_schedule_html` - דפדוף יום-אחר-יום (חצים/סוויפ) על פני לוח העונה המלא
(`fetch.get_season_schedule`), נפתח תמיד ב"היום" האמיתי של הצופה (שעון ישראל, ראו
`config.ISRAEL`). ימי עבר מציגים תוצאה - עשירה (עם קישור לתקציר יוטיוב, תג הארכה/גביע וכו')
ליום שהברייף הזה עצמו מדבר עליו, פשוטה (תוצאה בלבד) לכל יום אחר. ימים עתידיים מציגים שעת
פתיחה בשעון ישראל. כפתור 📅 בסרגל הניווט פותח לוח חודש (מוגבל לטווח החודשים שיש בהם באמת
נתונים) - יום עם משחקים מסומן במספר בעיגול כתום, לחיצה עליו קופצת ישירות לאותו יום.

כותרת הטאב וסרגל הדפדוף `position:fixed` (לא `sticky`) - `sticky` נשבר בפועל בטלטול מגע
אמיתי (לא נראה בבדיקה סינתטית, רק במכשיר אמיתי) כשהעמוד גם מטפל בעצמו במחוות סוויפ
אופקיות. כל מעבר בין ימים (סוויפ, חצים, או קפיצה מהלוח החודשי) מאפס את גלילת `<main>`
לראש הרשימה - אחרת יום חדש נפתח באותו מיקום גלילה של הקודם, עם המשחקים הראשונים חתוכים
מעל המסך.

### עונה רגילה
טבלת ליגה (מזרח/מערב, ממוינת לפי `PlayoffRank`, כולל סטריק). Storylines: משחקים צמודים/
הארכה, ביצועים חריגים (30+ נק', טריפל-דאבל, 15+ ריב'/אסיסט), הפתעות (קבוצה מדורגת נמוך
שניצחה גבוה), סטריקים בולטים.

### NBA Cup (Emirates NBA Cup)
כל משחק מתויג ברמת משחק בודד (`cup_subtype`: `"in-season"`=בתים, `"in-season-knockout"`
=רבע גמר ומעלה). טבלת הליגה הרגילה מוצגת תמיד בימי גביע (חוץ מהגמר, כל משחקי הגביע נספרים
בעונה הרגילה).
- **שלב הבתים**: טאב "בתי הגביע" - `Group A/B/C` לכל קונפרנס, מאזן+הפרש סלים בגביע בלבד,
  קו מפריד בין מקום 1 ל-2. מסומנת ה-Wild Card (הסגנית עם המאזן הטוב ביותר בקונפרנס, עולה
  גם היא לנוקאאוט) בתג "WC".
- **שלב הנוקאאוט**: טאב "בראקט הגביע" - בראקט מחובר אמיתי (רבע-גמר→חצי-גמר→גמר), תמיד
  מציג את הצורה המלאה (TBD למשבצות לא-מוכרעות). כותרות עמודות באנגלית ("Quarterfinals/
  Semifinals/Championship"). ה-wins/losses שמדווח ה-API בשלב הנוקאאוט הם **מוגבלים לשלב
  הנוקאאוט עצמו** (למשל 1-0), לא מאזן העונה - בטאב התוצאות מוצג המאזן האמיתי (נשלף מהטבלה).

### פלייאוף
טאב אחד - "בראקט הפלייאוף" (`_build_combined_playoff_bracket_html`) - **לא** שני טאבים
נפרדים לכל קונפרנס. בראקט מחובר אחד שמכיל את שני הקונפרנסים (מערב למעלה, מזרח למטה) פלוס
גמר ה-NBA, מדפדף (חצים/סוויפ) בין 3 עצירות של 2 סיבובים בכל פעם (רצועה רציפה אחת לכל
קונפרנס, לא בניית-מחדש בכל עצירה - סיבוב משותף לשתי עצירות סמוכות נשאר אותו אלמנט, רק
זז). נפתח תמיד בעצירה הרלוונטית ללילה הזה. הזיווג בסיבוב 1 (1נגד8+4נגד5 בחצי אחד; 2נגד7+
3נגד6 בחצי השני) הוא מבנה בראקט אמיתי, לא שרירותי. כל תא מציג מספר seed (בתג ממוסגר) ליד
קוד הקבוצה, והקבוצה עם **יתרון הבית** (seed נמוך יותר) מוצגת תמיד למעלה - לא לפי מי מוביל
כרגע בניצחונות.
**גמר NBA**: כשיש סדרת גמר פעילה, רצועת הגמר המשותפת (מוצגת בעצירה השלישית) מחוברת
בקו-חיבור לשתי תיבות גמר הקונפרנס. התגים בתא הזה הם W/E צבעוניים (לא seed - לא ניתן
להשוואה בין קונפרנסים), עם הודעת "אלופת ה-NBA!" כשהסדרה מוכרעת.
**"תוצאת המשחק"**: כשיש רק משחק אחד בלילה (נפוץ בפלייאוף/גמר), כותרת האקורדיון הראשון
משתנה בהתאם, וגם הטקסט החופשי (הנחיה ב-SYSTEM_PROMPT) כותב בלשון יחיד.
**באג ידוע, לא תוקן**: תיבת גמר ה-NBA חתוכה כ-55px בקצה הימני שלה בעצירה השלישית - קיים
גם בקוד המקורי, לא נגרם משינוי כלשהו. שני ניסיונות תיקון (הזזה שטוחה, הזזה מוכפלת) גרמו
לחיתוך גרוע יותר של אותה תיבה וחזרו לאחור. דורש בדיקה מחדש של חישוב הרוחב של רצועת הגמר
(`positionFinalsConnector`, `.nba-finals-track-wrap`) לפני ניסיון נוסף.

### פלייאין
טאב אחד - "פלייאין" - **לא** שני טאבים נפרדים. שני בראקטים של קונפרנס (מערב, אז מזרח)
מוצגים אחד מתחת לשני **באותו טאב**, בלי דפדוף/pager ביניהם (`_build_play_in_html` פשוט
נקרא פעמיים ומוצב בזה אחר זה). מוצג כבראקט: 7-מול-8 ו-9-מול-10 מתכנסים למשחק הכרעה.
זיהוי משחק פלייאין: `game_id` מתחיל ב-`"005"` (`poRoundDesc` ריק אצלו, אז `is_playoffs`
נשאר False). זיהוי שלושת המשחקים לפי `seed` הקבוצות, לא לפי תאריך (הקונפרנסים לא תמיד
משחקים באותו לילה). **הבראקט לא סימטרי**: מנצח 7-מול-8 מקבל seed 7 ומסיים (לא ממשיך) -
המפסיד הוא זה שממשיך למשחק ההכרעה. לכן כל תא מקבל caption קטן מתחתיו שמסביר את המשמעות
המדויקת. `get_play_in_bracket()` ב-`fetch.py` משחזר את **כל** משחקי הפלייאין של העונה עד
התאריך המבוקש (לא רק לילה בודד) - כדי שבלילה של ההכרעה, שני המשחקים הקודמים לא יוצגו
כ-TBD. אין משחק "if necessary" - תמיד בדיוק 3 משחקים קבועים לכל קונפרנס. אין מאזן עונה
מוצג בטאב התוצאות לימי פלייאין (לא רלוונטי). `summarize.py` מקבל הקשר מובנה
(`play_in_context` עם `game_type`: `seven_vs_eight`/`nine_vs_ten`/`decider`) כדי לכתוב
משפט נכון על מה בדיוק על הפרק.

### טבלת ליגה - קוד קבוצה במקום שם חתוך
`.standing-team` (וגם `.cup-group .standing-team`) מקבל `data-fullname`/`data-tricode` -
`fetch.py` מצמיד `Tricode` לכל שורת טבלה (join על השם המלא מול `season_schedule`, כי
ל-`LeagueStandingsV3` עצמו אין עמודת tricode). JS (`checkTeamTruncation()`) מודד
`scrollWidth` מול `clientWidth` בפועל אצל הצופה הספציפי (תלוי מכשיר/גופן/זום נגישות - לא
ניתן לדעת ברינדור) ומחליף לקוד 3 האותיות רק אם השם באמת נחתך, מחזיר לשם המלא אם כבר לא.
רץ בטעינה, ב-resize, בשינוי גודל טקסט נגישות, ובכל פתיחת טאב/אקורדיון. `aria-label` תמיד
נשאר השם המלא, ללא קשר לטקסט המוצג.

### הקשר מעבר למשחק (פציעות + כוכבים חלשים)
`get_injuries()` (ESPN) ו-`get_player_power_ratings()` (נוסחת "ESPN Rating" מחושבת
מ-`leaguedashplayerstats`: `PTS+REB+1.4*AST+STL+1.4*BLK-.7*TOV+FGM+.5*FG3M-.8*(FGA-FGM)
+.25*FTM-.8*(FTA-FTM)`) - שניהם עם fallback שקט (רשימה ריקה) אם מקור לא-רשמי נופל.
`find_injury_notes` (שחקן `Out` מקבוצה ששיחקה) ו-`find_star_weak_performances` (שחקן
בין 30 המובילים בדירוג שקלע חצי או פחות מהממוצע העונתי **שלו עצמו**). הדירוג המשוקלל
משמש רק פנימית לזיהוי "כוכב" - **לא** נכלל ב-JSON שנשלח ל-Claude ואסור לציין מספר דירוג
בטקסט. **לא ממומש**: זיהוי "חזרה מפציעה אחרי היעדרות ארוכה" או "נפצע באמצע המשחק" - דורש
מקור נתונים נוסף (game log/עדכון בזמן אמת) שלא קיים כרגע.

### הגדרות ונגישות
כפתור יחיד בכותרת (⚙️, `.settings-toggle`) פותח פאנל הגדרות אחד שמרכז הכול: toggle
בהיר/כהה, 3 רמות גודל טקסט (`--a11y-text-scale`, לא CSS zoom), כפתור רענון ידני (🔄 - עוקף
HTTP cache עם query param, נחוץ כי PWA ב-standalone על iOS מבטל pull-to-refresh), קישור
לדמואי הבטא, יצירת קשר (GitHub Issues + מייל), והצהרת נגישות. Focus trap מלא בפאנל (Tab
לא בורח, Escape סוגר). Landmarks סמנטיים (`header`/`main`/`footer`), `:focus-visible` על
כפתורים/קישורים, `.sr-only`. הצהרת הנגישות מתארת מה שבוצע בפועל, בלי הצהרת פטור משפטית -
זה כלי אישי לא שירות מסחרי.

### כללי ניסוח וכתיב (תהליך מתמשך)
`summarize.py`'s SYSTEM_PROMPT כולל מילון תעתיק קבוע לשמות כל 30 הקבוצות ולשחקנים נפוצים
(יאניס אנטטקומפו לא ג'יאניס, סלטיקס לא קלטיקס, דנבר לא דנוור וכו'), בחירות מילים ("רצף"
לא "סטריק", "מומנטום" נשאר), הנחיה לצמצם קישוטי לשון מיותרים, ועובדות קבועות (גמר הגביע
תמיד ב-T-Mobile Arena לאס וגאס). מתעדכן ככל שמגיעים תיקונים נוספים.

## החלטות
- שפת הפלט: עברית בלבד.
- ערוץ הפצה: PWA מאורח ב-GitHub Pages (לא מייל/Slack/Telegram).
- קבוצות מועדפות: כרגע כללי - בעתיד אולי העדפות משתמש (קבוצות/שחקנים אהודים).

## משימות פתוחות (Backlog)
לא לבצע עד שיתבקש במפורש:

1. **טאב "מרחף" קבוע** - רעיון גולמי, בלי חזון מלא עדיין: טאב שנשאר תמיד זמין בלי קשר
   לסוג היום (למשל טבלת הליגה). כרגע הכל בתוך אקורדיונים שצריך לפתוח אחד-אחד.
2. **ארכיון עונתי** - חזרה אחורה לעיין בבריפים של ימים קודמים, מסודר לפי תאריכים.
   `output/index.html` מציג תמיד רק את האחרון - `demos.html` מכסה 5 ימים נבחרים בלבד, לא
   תחליף לארכיון אמיתי. צריך עמוד ארכיון קבוע שיתעדכן אוטומטית עם כל בריף חדש.
3. **הפעלת Task Scheduler** - המשימה `FullCourtBrief` קיימת אבל Disabled - להפעיל
   (`Enable-ScheduledTask`) כשמחליטים שרוצים ריצה אוטומטית בפועל.
4. **Push Notifications** - התראה לטלפון כשבריף חדש מוכן. היתכן (iOS 16.4+, רק ל-PWA
   מותקן) - חסר Service Worker + מנגנון subscription. יתרון: `scheduler.py` יכול לשלוח
   בעצמו (רץ מהבית עם אינטרנט אמיתי), אין צורך ב-backend נפרד - למשתמש יחיד זה פשוט
   משמעותית ממוצר רב-משתמשים (בלי מסד נתונים/הרשמה אוטומטית/ניקוי מנויים).
5. **הפרדה בין גרסת מפתח לגרסת לקוח** - לקראת הפיכת הכלי ממשהו אישי למוצר סופי, להפריד
   בין "גרסת מפתח" (רק למשתמש עצמו) לבין "גרסת לקוח" (המוצר הסופי). רעיון גולמי, בלי
   פרטים על מה בדיוק שונה בין הגרסאות (פ'יצרים? ענפי git? תצורת build? קהל יעד?) - לפרט
   כשמתחילים לעבוד על זה.
