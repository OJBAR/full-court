# NBA Morning Brief — Project Brief

## מה בונים
כלי שרץ כל בוקר (שעון ישראל) ומייצר סיכום קריא של לילה של NBA:
- מה קרה במשחקים שהסתיימו בלילה (תוצאות, רגעים בולטים, ביצועים חריגים)
- מה לצפות בערב הקרוב (לוח משחקים, matchups מעניינים)

## מקור נתונים
**⚠️ עודכן:** התוכנית המקורית התבססה על balldontlie API, אבל הם שינו את מודל התמחור —
בטייר החינמי נשארו רק `teams`/`players`/`games`; box scores, player stats ו-standings
נעולים כיום מאחורי תשלום ($9.99-$39.99/חודש). לכן עברנו למקור חינמי אחר.

**nba_api** (פייתון, קוד פתוח) — https://github.com/swar/nba_api
- עוטף את ה-endpoints הרשמיים הבלתי-מתועדים של stats.nba.com
- חינמי לגמרי, בלי API key, בלי הרשמה
- אין rate limit רשמי — מומלץ self-throttling של ~1 בקשה/שנייה (delay בין קריאות)
- מודולים רלוונטיים (`nba_api.stats.endpoints`):
  - `scoreboardv2(game_date=...)` — משחקים לפי תאריך + תוצאות
  - `boxscoretraditionalv2(game_id=...)` — בוקס סקור מלא (נק', ריבאונד, אסיסט וכו') למשחק בודד
  - `leaguegamefinder` — חיפוש משחקים בטווח תאריכים
  - `leaguestandingsv3()` — טבלה עדכנית (לזיהוי הפתעות)
- שימו לב: צריך למשוך `game_id` לכל משחק מ-scoreboard/gamefinder לפני קריאה ל-box score

⚠️ **טיפול ב-timezone (פושט)**: אין צורך בהמרה זהירה בין ישראל לארה"ב — הכלי ירוץ פעם ביום
בשעה קבועה (לאחר שכל משחקי הלילה הסתיימו, בערך 9-10 בבוקר שעון ישראל), ולכן מספיק לקחת
את "אתמול" בשעון US Eastern ישירות דרך `zoneinfo`, בלי שלב ביניים של שעון ישראל.

## ארכיטקטורה מוצעת

```
nba_morning_brief/
├── CLAUDE.md              # קובץ זה
├── config.py              # API key, timezone settings
├── fetch.py               # שליפת games / box_scores / stats / standings
├── storylines.py          # זיהוי סיפורים מעניינים מהנתונים הגולמיים
├── summarize.py           # קריאה ל-Claude API כדי לכתוב את הסיכום בעברית/אנגלית
├── render.py              # פורמט הפלט (Markdown / HTML)
├── main.py                # מריץ את כל הצינור בסדר
└── output/
    └── YYYY-MM-DD.html    # הסיכום היומי (ניוזלטר HTML)
```

## שלב 1 (להתחיל כאן)
1. התקנת `nba_api` (`pip install nba_api`) — אין צורך ב-API key או הרשמה
2. `fetch.py`: פונקציה ששולפת את משחקי "אתמול בלילה" (עם timezone נכון) + הbox scores שלהם
3. הדפסת JSON גולמי למסך כדי לוודא שהנתונים נכונים לפני שממשיכים

## שלב 2
`storylines.py`: לוגיקה שסורקת את הבוקס סקורים ומזהה:
- משחקים שהוכרעו בהפרש קטן (≤5 נק') או ב-OT
- ביצועים חריגים (30+ נק', טריפל-דאבל, 15+ ריבאונד/אסיסט)
- הפתעות: קבוצה מדורגת נמוך שניצחה קבוצה מדורגת גבוה
- סטריקים (ניצחונות/הפסדים רצופים)

## שלב 3
`summarize.py`: שליחת ה-storylines כ-JSON מובנה ל-Claude API עם system prompt שמנחה לכתוב
סיכום תמציתי, בעברית, בטון של כתב ספורט — לא רשימת באלטים יבשה.

## שלב 4
`render.py` + `main.py`: פלט לקובץ HTML מעוצב (ניוזלטר, נפתח בדפדפן) ל-`output/YYYY-MM-DD.html`,
ואופציה ל-scheduled task שמריץ כל בוקר.

**עיצוב (הוחלט)**: mobile-first (המשתמש קורא עם קפה בבוקר, בעיקר מהטלפון), RTL מלא, ערכת
נושא כהה. מתחת לסיכום הראשי - שני אלמנטים מתקפלים (`<details>`/`<summary>`, בלי JS):
"כל תוצאות הלילה" (רשימת כל המשחקים) ו"הטבלה" (מזרח/מערב, ממוינים לפי PlayoffRank,
כולל סטריק). יש גם "רשת ביטחון" שמנקה שאריות Markdown (# ו-**) מהטקסט של Claude
לפני שהוא נכנס ל-HTML, כי המודלים לא תמיד מצייתים במדויק להנחיה "בלי Markdown".

## החלטות פתוחות
- שפת הפלט: **עברית בלבד** (הוחלט - שונה מ"אנגלית" שהוחלט קודם)
- ערוץ הפצה: **קובץ מקומי (HTML)** (הוחלט) — עיצוב כמו ניוזלטר, נפתח בדפדפן. לא מייל/Slack/Telegram בשלב זה.
- קבוצות מועדפות: **כרגע כללי** — בעתיד תתווסף אפשרות להעדפות משתמש (קבוצות/שחקנים אהודים)

## רשימת משימות עתידיות (Backlog)
לא לבצע עד שיתבקש במפורש - רק לזכור שהן על השולחן:

1. ~~**מצב פלייאוף**~~ — **בוצע.** `fetch.py`: `get_playoff_series()` משחזר את מצב כל הסדרות
   הפעילות מתוך `leaguegamefinder` (endpoint שלא נתן בעצמו "בראקט" מוכן). זיהוי מצב פלייאוף
   דרך `is_playoffs` (נגזר משדה `poRoundDesc` שכבר קיים ב-`ScoreboardV3`). `storylines.py`:
   גלאים ייעודיים - `find_playoff_seed_upsets` (הפתעה = seed גבוה שניצח seed נמוך בפער,
   לא סטריקים מסוף העונה) ו-`find_series_developments` (סגירת סדרה / תיקו לקראת משחק מכריע).
   `summarize.py`: system prompt מותאם שמדגיש הקשר סדרה/יתרון/הדחה במצב פלייאוף.
   **עודכן משמעותית - שני טאבים נפרדים במקום תצוגה אחת משולבת:** "בראקט מזרח" ו"בראקט
   מערב" (`_build_conference_bracket_html`), כל אחד בראקט מחובר אמיתי מסיבוב 1 ועד גמר
   הקונפרנס (3 עמודות: 1st Round עם 4 סדרות / Conf. Semifinals עם 2 / Conf. Finals עם 1) -
   **מוצג תמיד**, כבר מרגע שסיבוב 1 מתחיל, לא רק אחרי שהוא נגמר. `fetch.py`: `get_playoff_series`
   עודכן למשוך גם `seed` לכל קבוצה (מ-`line_score`, לא רק round/conference כמו קודם) - **חיוני**
   כי הזיווג בסיבוב 1 בין 4 הסדרות לתוך 2 הקבוצות (Pair A / Pair B) שמתכנסות לחצי-גמר
   הקונפרנס הוא לפי מבנה בראקט אמיתי (1נגד8 ו-4נגד5 נפגשים בחצי אחד; 2נגד7 ו-3נגד6 בחצי
   השני) - **לא ניחוש שרירותי**. נבדק מול נתוני 2026 אמיתיים ותואם בדיוק. ה-seed נשאר קבוע
   גם כשהקבוצה מתקדמת, אז אותה לוגיקת שיוך-לחצי עובדת גם על סדרות Conf. Semifinals.
   מיקום הקבוצות (1-8) כבר תמיד מוצג נכון - ה-seed בפלייאוף עצמו, לא הדירוג בעונה הרגילה
   (קבוצה שהגיעה מפלייאין עם seed=8 מוצגת ככה, גם אם סיימה 9/10 בעונה). **הערה**: בניגוד
   לבראקט הגביע, אין כאן "הקרנה קדימה" של מנצחות ידועות לשלב הבא (למשל אם שתי סדרות סיבוב 1
   כבר הוכרעו, חצי-הגמר עדיין TBD עד שיש נתוני משחק אמיתיים של Conf. Semifinals) - לא התבקש,
   אפשר להוסיף בעתיד באותה שיטה כמו בגביע אם ירצו. **גם לא כלול**: גמר ה-NBA עצמו (רק עד
   גמרי הקונפרנס לכל היותר, כמבוקש) - עמוד ייעודי לימי פלייאין הוא משימה נפרדת לעתיד.
   דמו לדוגמה: `demo_playoffs.py` + `demo_playoffs_fixture.json` (25/4/2026, סיבוב 1 -
   שני הטאבים מציגים 4 סדרות אמיתיות כל אחד וחצי-גמר/גמר קונפרנס כ-TBD). `demo_playoffs_round2.py`
   + `demo_playoffs_round2_fixture.json` (13/5/2026) - סדרות חצי-גמר כבר קיימות, מדגים גם
   תא אמיתי וגם TBD לגמר הקונפרנס. שני ה-fixtures רועננו משליפה חדשה כדי לכלול את שדה
   ה-seed (לא היה קיים בגרסה הישנה).
2. **טעויות כתיב וניסוח** — המטרה: שהטקסט שנוצר יישמע טבעי, לא כמו שקלוד כתב אותו
   "בעצלנות". **סבב ראשון בוצע**: המשתמש שלח כמה דמואים מתוקנים/כתובים בעצמו (עונה
   רגילה, בתי גביע, נוקאאוט גביע, פלייאוף) - נאסף כ"עבודת רקע" בלי דיון בזמן אמת על
   כל תיקון (לפי בקשה מפורשת). סונתז לתוך `summarize.py`'s SYSTEM_PROMPT כבלוק חדש
   "כללי ניסוח וכתיב": מילון תעתיק קבוע לשמות כל 30 הקבוצות ולכמה שחקנים נפוצים
   (יאניס אנטטקומפו לא ג'יאניס, ראסל ווסטברוק לא ורסל, סלטיקס לא קלטיקס, דנבר לא
   דנוור וכו') - הדגש המרכזי שהמשתמש ביקש כדי לא לתקן את אותו דבר שוב ושוב; בחירות
   מילים ספציפיות (רצף לא סטריק, אבל מומנטום כן נשאר - לא כל לועזית רעה); הנחיה
   לצמצם קישוטי לשון מיותרים (אמיתי/פרועה/ציון דבר שכבר ברור מהתוצאה); תיקון דקדוקי
   אחד ("שלטו רוב הדרך" לא "מרוב"); ועובדה קבועה שגמר הגביע תמיד בלאס וגאס. **פתוח**:
   זה תהליך מתמשך - עוד דוגמאות/תיקונים יכולים להגיע בעתיד ולהצטרף לרשימה.
3. ~~**נגישות**~~ — **בוצע.** דגש על עיוורים/קוראי מסך, לא רק ניגודיות צבעים. `render.py`:
   כפתור `.a11y-toggle` (♿, סגנון תאום ל-`.theme-toggle` הקיים, בצד הנגדי) פותח פאנל
   נגישות (`role="dialog" aria-modal="true"`) עם בקרת גודל טקסט - 3 כפתורי "א" בגדלים
   עולים (כמו בתקן הנגישות הישראלי המוכר), דרך `zoom` על ה-`.wrapper` ו-attribute
   `data-a11y-fontsize` על ה-`<html>`, נשמר ב-localStorage כמו `.theme-toggle`. הפאנל כולל focus trap מלא (Tab לא בורח מהפאנל,
   Escape סוגר ומחזיר פוקוס לכפתור שפתח, לחיצה על הרקע סוגרת). שיפורי נגישות נוספים
   ברמת העמוד: אלמנטי landmark סמנטיים (`<header>`/`<main>`/`<footer>` במקום `<div>`
   גנרי, עוזר לניווט בקוראי מסך), `:focus-visible` עם קונטור בצבע ה-accent על כפתורים/
   קישורים/`<summary>` (ניווט מקלדת ברור), ומחלקת עזר `.sr-only`. `<details>`/`<summary>`
   הקיימים כבר נגישים באופן טבעי (ולכן זו הייתה הסיבה המקורית לבחור בהם). **עודכן:** נוסף
   גם קישור "הצהרת נגישות" בתחתית פאנל הנגישות (מתחת לבקרת גודל הטקסט), שפותח פופ-אפ שני
   (`#a11y-statement-overlay`, אותו מנגנון פאנל/focus-trap, כללי - נגישות כתוב מה שבוצע
   בפועל, בלי הצהרת תאימות משפטית פורמלית, כי זה כלי אישי ולא שירות ציבורי/מסחרי).
4. ~~**NBA Cup (Emirates NBA Cup / In-Season Tournament)**~~ — **בוצע (כולל 3 התיקונים).**
   `fetch.py`: כל משחק מתויג ברמת משחק בודד (`cup_subtype`: `"in-season"` לשלב הבתים /
   `"in-season-knockout"` לרבע גמר ומעלה, `cup_sub_label` לשם הבית/שלב). `get_cup_bracket()`
   בונה את הבראקט המלא מ-`ScheduleLeagueV2` + `get_games_for_date` לתוצאות בפועל - בניגוד
   לפלייאוף, אין צורך בשחזור סדרות כי גביע הוא הדחה חד-פעמית. `get_cup_group_standings()`
   מחשב מאזן נצחונות-הפסדים והפרש סלים **בתוך משחקי הבתים בלבד** (לא העונה הרגילה),
   מקובץ לפי בית (`gameSubLabel`, למשל "East Group C"). דגלי יום `is_cup_knockout` ו-
   `is_cup_groups` קובעים אילו טאבים מוצגים. `storylines.py`: `find_cup_advancements`
   (כל משחק נוקאאוט = גם הדחה וגם התקדמות). `summarize.py`: הנחיה שונה לשני המצבים - שלב
   בתים מקבל אזכור קליל בלבד (זו עדיין עונה רגילה), נוקאאוט מקבל דגש דרמטי מלא.
   `render.py` (3 התיקונים שבוצעו):
   - **טבלת הליגה הרגילה מופיעה תמיד בימי גביע** (בתים וגם נוקאאוט) - חוץ מהגמר, כל
     משחקי הגביע נספרים בעונה הרגילה. בפלייאוף הטבלה עדיין מוחלפת לגמרי בסדרות (ללא שינוי).
   - **בימי בתים: טאב שלישי** - "בתי הגביע" (`_build_cup_group_standings_html`), מציג
     `Group A/B/C` לכל קונפרנס, עם קבוצה, מאזן נצחונות-הפסדים בגביע בלבד, הפרש סלים,
     וקו מפריד (`.boundary`) בין מקום 1 ל-2 בכל בית.
   - **בראקט הנוקאאוט הוא עכשיו בראקט אמיתי מחובר** (`_build_cup_bracket_html`, מבנה
     `.bracket > .bracket-round > .bracket-pair > .bracket-match` עם קווים מחברים ב-CSS) -
     שני נצחונות רבע-גמר מאותה קונפרנס מתחברים לחצי-הגמר שלה, ושני חצי-הגמר מתחברים לגמר.
   **עודכן:** בטאב "בתי הגביע" מסומנת גם ה-Wild Card - הסגנית (מקום 2 בבית) עם השיא הטוב
   ביותר מתוך כל הסגניות בקונפרנס (השוואה חוצה-בתים לפי מאזן ואז הפרש סלים), כי היא זו
   שעולה לנוקאאוט יחד עם שלוש המנצחות. מסומנת בתג "WC" קטן ליד השם, עם הסבר קצר בתחתית
   הטאב.
   שני דמואים: `demo_cup_groups.py` (14 בנוב' 2025, 9 משחקים) ו-`demo_cup_knockout.py`
   (16 בדצמ' 2025, ליל הגמר). **הערה:** קבצי ה-fixture הקיימים (`demo_cup_*_fixture.json`)
   נוצרו לפני התיקונים ולא כוללים `is_cup_groups`/`cup_group_standings` - לכן טאב "בתי
   הגביע" לא יופיע עד שה-fixture ירוענן משליפה חדשה של `fetch_for_date`.
5. **הקשר מעבר למשחק** (פציעות, תשומת לב מיוחדת לביצוע חלש של שחקן מרכזי) — **בוצע
   חלקית.** בדיקת היתכנות אמיתית: nba_api עדיין לא כולל דוח פציעות, אבל ESPN חושף שני
   endpoints לא-רשמיים (בלי מפתח, אותה רוח בדיוק כמו nba_api עצמו):
   - `site.api.espn.com/.../nba/injuries` — דוח פציעות עדכני (סנאפשוט חי, לא לפי תאריך),
     כולל שם שחקן/קבוצה/סטטוס (`Out`/`Day-To-Day`) והסבר קצר וארוך.
   - הדף `espn.com/nba/seasonleaders` חשף נוסחת "ESPN Rating" ציבורית - דירוג משוקלל
     יחיד (לא נפרד לפי נק'/ריב'/אסיסט): `PTS + REB + 1.4*AST + STL + 1.4*BLK - .7*TOV +
     FGM + .5*FG3M - .8*(FGA-FGM) + .25*FTM - .8*(FTA-FTM)`. **הוחלט לחשב את זה בעצמנו**
     מ-`leaguedashplayerstats` של nba_api (שכבר יש לו את כל הרכיבים) במקום להזדקק ל-ESPN
     גם בשביל זה - פחות תלות בספק לא-רשמי נוסף. נבדק מול נתונים אמיתיים: 5 הראשונים
     בעונת 24-25 יצאו יוקיץ'/יאניס/שיי/לוקה/וומבניאמה - תואם ציפייה.

   **מה קיים בפועל**: `fetch.py`: `get_injuries()` (ESPN) ו-`get_player_power_ratings()`
   (nba_api + הנוסחה) - שניהם עם try/except ב-`fetch_for_date` שמתדרדר בעדינות (רשימה
   ריקה) אם אחד מהמקורות הלא-רשמיים נופל, בלי להפיל את כל הריצה. `storylines.py`:
   `find_injury_notes` (שחקן מסומן `Out` מקבוצה ששיחקה) ו-`find_star_weak_performances`
   (שחקן בין 30 המובילים בדירוג המשוקלל שקלע הלילה חצי או פחות מהממוצע העונתי **שלו
   עצמו** - יחסי לשחקן, לא סף מוחלט קבוע לכולם). **חשוב**: הדירוג/רייטינג המשוקלל
   משמש רק פנימית ב-`storylines.py` כדי להחליט מי "נחשב כוכב" - הוא לא נכלל ב-JSON
   שנשלח ל-Claude (רק `season_avg_points`, שזה נתון אמיתי ומותר לצטט), וה-system
   prompt מפורש שאסור לציין מספר דירוג בטקסט. שניהם פועלים בכל מצב (עונה רגילה/
   פלייאוף/גביע). `summarize.py`: system prompt מוסבר להשתמש ב-`comment`
   כמו שהוא בלי להמציא פרטים, ולא לנחש "למה" הייתה סטייה חלשה אלא אם יש גם injury_note
   תואם לאותו שחקן.

   **מה עדיין לא קיים** (נשאר פתוח): "חזרה מפציעה אחרי היעדרות ארוכה" (כמו הדוגמה של
   סטף קרי) - דוח הפציעות של ESPN הוא סנאפשוט *נוכחי* בלבד, לא נותן היסטוריה של כמה
   משחקים שחקן פספס - צריך מקור נוסף (game log לכל שחקן) כדי לזהות "חזר אחרי חודש",
   ולא מומש עדיין. גם "נפצע באמצע המשחק ולא חזר" (כמו הדוגמה של לברון) נשאר בלתי אפשרי
   בלי מקור בזמן-אמת - שדה ה-comment בבוקס סקור קיים רק לשחקנים עם 0 דקות, לא לשחקן
   שיצא באמצע.
6. ~~**תזמון דינמי**~~ - **בוצע.** `scheduler.py`: פונקציית `check()` מיועדת להיות
   מופעלת כל כ-20 דקות (למשל דרך Windows Task Scheduler) - כל הרצה היא בדיקה קצרה
   וחסרת מצב ("stateless"): "האם הסיכום של אתמול בלילה מוכן להישלח?" בלי sleep/לולאה
   פנימית - קצב הבדיקה מגיע מהטריגר החוזר של Task Scheduler, לא מקוד שמחכה. הגנת
   אידמפוטנטיות: אם `output/{date}.html` כבר קיים, יוצא מיד. **אם אין משחקים בלילה
   היעד בכלל - לא רץ בכלל, בלי סיכום ובלי קובץ** (תוקן אחרי בדיקה - בהתחלה כתבתי שזה
   ירוץ מיד גם בלי משחקים, המשתמש תיקן).
   במקום שעה קבועה, לבדוק לפי שעת התחלת המשחק
   האחרון של הלילה (US Eastern) - נשלף ישירות מ-`gameTimeUTC` ב-ScoreboardV3 (לא נדרשה
   קריאת API נוספת ל-`ScheduleLeagueV2`):
   - בדיקה ראשונה ב-start+2:30 שעות (`FIRST_CHECK_MINUTES`). לפני זה `check()` יוצא מיד
     בלי לבדוק כלום. **אחרי זה, כל הרצה היא אותה בדיקה בדיוק** (אין הבדל קוד בין "בדיקה
     שנייה" ל"בדיקה עשירית") - קצב הבדיקה (20 דקות) נקבע על ידי התדירות שבה Task
     Scheduler מפעיל את הסקריפט, לא על ידי לוגיקה פנימית.
   - **המנגנון האמיתי לא מנחש משך משחק בכלל** - הוא בודק בכל פעם את הסטטוס האמיתי מול
     ה-API (`gameStatusText`/`gameStatus`), אז לא יכול "לפספס" משחק שנמשך הרבה זמן (אפילו
     הארכות מרובות) - הוא פשוט ימשיך לבדוק עד שיראה Final. מודל משך-משחק (135 דק' רגיל,
     160 דק' הארכה אחת) שימש **רק לסימולציה ההיסטורית** (ראו למטה), לא לקוד האמיתי.
   - **רצפה**: הסיכום לעולם לא נשלח לפני 5:30 בבוקר שעון ישראל, גם אם הבדיקות מסתיימות
     מוקדם יותר (למשל לילה עם משחק בודד ומוקדם).
   - **טיפול בדחיית משחק**: אם `gameStatusText` של משחק מסוים מראה דחייה/ביטול, המנגנון
     לא ממתין לו בכלל - מוציא אותו מהרשימה מיד וממשיך. רשת ביטחון אחרונה (לא העיקרית):
     תקרת זמן גמישה מאוד (כמה שעות) למצב לא צפוי שלא זוהה כדחייה - אם מגיעים אליה, שולחים
     את הסיכום בלי המשחק הבעייתי ומציינים זאת, לא נתקעים בשקט.
   - **סימולציה שבוצעה** (לא קוד, רק ניתוח על נתוני 2025-26 האמיתיים דרך `ScheduleLeagueV2`
     + בדיקת סטטוס Final/OT האמיתי לכל לילה): רוב הלילות (כ-95%) מגיעים בין 05:30 ל-08:30
     שעון ישראל, הכי נפוץ 07:30 (כ-31%). חריגים אמיתיים: 7/4/2026 (משחק אחרון התחיל 23:00
     ET, המאוחר ביותר בעונה) ו-8/2/2026 (משחק בודד מוקדם ב-15:00 ET, יום לפני האולסטאר -
     בלי הרצפה היה יוצא ב-00:30 שעון ישראל). לילות עם הארכה בודדת (כ-4% מהעונה) מתעכבים
     כ-20-30 דקות נוספות. **חשוב**: כשמדווחים על השעות האלה - תמיד לציין ET מול שעון ישראל
     במפורש, כי שעה בודדת (כמו "22:30") מתפרשת אחרת לגמרי בכל אזור זמן.
7. ~~**תיקוני UI קטנים**~~ - **בוצע.**
   - כותרת שלב מעל כל עמודה בשני הבראקטים - `_bracket_column_html()` עוטפת כל עמודה
     ב-wrapper חדש (`.bracket-column`) עם כותרת (`.bracket-round-label`): "חצי גמר /
     גמר קונפרנס / גמר NBA" בפלייאוף (עברית). **בגביע - באנגלית לפי בקשה מפורשת:**
     "Quarterfinals / Semifinals / Championship" (לא "רבע גמר" וכו').
   - **תוקן** (התגלה מצילום מסך): `_build_cup_bracket_html` היה מציג רק את השלבים/
     המשחקים שכבר שוחקו בפועל ומדלג לגמרי על השאר - כך שבתחילת שלב הנוקאאוט (למשל
     רק 2 מתוך 4 משחקי רבע-גמר שוחקו) הבראקט הציג עמודה יחידה בלי שום רמז לצורה
     המלאה. עכשיו, בדיוק כמו `_build_playoff_bracket_tree_html`, הפונקציה משתמשת
     ב-`_pad_series` ומציגה תמיד את הצורה המלאה (4 רבע-גמר / 2 חצי-גמר / 1 גמר),
     עם placeholder מעומעם "TBD" (`.bracket-match-tbd`) לכל משבצת שעוד לא נקבעה.
     `_bracket_match_html` עודכן לקבל `game: dict | None`.
   - ליד התאריך בעמוד נוסף ", הלילה בין [יום] ל[יום]" (מחושב דינמית ב-`render()` לפי
     יום השבוע, `_HEBREW_WEEKDAYS`). **שימו לב**: זה נפרד מ-`{display_date}` שנשאר בלי
     שינוי בתג ה-`<title>` (רק תאריך, כמו שהוחלט - בלי הטקסט הנוסף שם).
8. ~~**הצהרת נגישות - עדכון לשימוש ציבורי**~~ - **בוצע.** הכלי אמור בסופו של דבר להיות
   מופץ כקישור בניוזלטר (לא רק לשימוש אישי). ב-`render.py` (`#a11y-statement-overlay`):
   הורד המשפט שמצהיר "כלי אישי, לא כפוף לחוק הפורמלי" - העמוד כבר לא מצהיר הצהרת פטור
   משפטית מהחוק (שאלה משפטית אמיתית שלא צריך "להכריע" בקוד), הניסוח מתאר רק מה בפועל
   נעשה. נוסף פלייסהולדר לקישור יצירת קשר בפוטר (`mailto:PLACEHOLDER@EXAMPLE.COM`, ליד
   "made by Ofek Barel") - **עדיין צריך להחליף בכתובת אמת** לפני הפצה בפועל (המשתמש טרם
   החליט אם זה יהיה המייל האישי שלו או כתובת ייעודית לפרויקט).
9. **עמוד ייעודי לימי פלייאין** - לא לבצע עד שיתבקש. כרגע ימי פלייאין (`poRoundDesc`
   כנראה "Play-In" או דומה - לא נבדק עדיין) לא מקבלים שום התייחסות ייעודית ב-`render.py`;
   `is_playoffs` כנראה False בשלב הזה (הפלייאין הוא לא רשמית "Playoffs" אלא שלב ביניים),
   כך שהם כרגע מוצגים כמו לילה רגיל בעונה. צריך: לבדוק בפועל איך `poRoundDesc`/`gameLabel`
   מזהים משחקי פלייאין, ולתכנן תצוגה מתאימה (כנראה משהו בין הצגת תוצאה בודדת לבין הקשר
   של "מי עולה/יורד" - 7-8 ו-9-10 בכל קונפרנס, עם משחק "if necessary" אפשרי).
10. **גמר NBA** - לא לבצע עד שיתבקש. הבראקטים החדשים (מזרח/מערב) עוצרים בגמר הקונפרנס
    בכוונה - הגמר עצמו (מזרח מול מערב) לא מקבל שום תצוגה ייעודית כרגע. צריך לתכנן טאב/סקשן
    נפרד לימי גמר (כנראה עם סטטוס הסדרה, יתרון בית וכו', בדומה למה שכבר קיים לכל סדרה
    בודדת בבראקטים).
11. **"תוצאת המשחק" ליום עם משחק בודד** - לא לבצע עד שיתבקש. כשיש רק משחק אחד בלילה
    (כרגע נדיר בעונה הרגילה, אבל שכיח בפלייאוף/גמר), הכותרת/ניסוח צריכים לשקף את זה
    בלשון יחיד ("תוצאת המשחק") ולא כמו כרגע (מנוסח כאילו יש כמה משחקים).
